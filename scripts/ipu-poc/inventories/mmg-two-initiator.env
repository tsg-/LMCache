# MMG NVMe-oF L2 benchmark inventory.
#
# This file names machines only. The coordinator derives the fixed benchmark
# mount and discovers the LMCache checkout and Python environment remotely.
INITIATOR_HOSTS=(mmgi0 mmgi1)
TARGET_HOST=mmgt
